window.onload = function () {
    let scale = window.devicePixelRatio * 100;

    if (scale > 125) {

        alert('Your screen resolution is exceeding 125% scale. Zoom out to 90% to render it correctly.');
   
    } else {
    }
};
